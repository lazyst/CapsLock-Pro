# Checklist

## 代码结构
- [x] 全局变量 `mouseModeEnabled` 已添加到 `lib/Globals.ahk`，并有清晰注释
- [x] 鼠标模式相关函数集中在 `lib/MouseControl.ahk` 中
- [x] 使用 `#HotIf mouseModeEnabled` 条件块管理鼠标模式热键
- [x] 复用现有的 `MouseMoveRelative`、`Click` 等函数

## 功能验证
- [x] `CapsLock+空格` 可以进入鼠标模式并显示提示"鼠标模式已启用"
- [x] 按 `Esc` 可以退出鼠标模式并显示提示"鼠标模式已关闭"
- [x] 按 `CapsLock` 可以退出鼠标模式并显示提示
- [x] 鼠标模式下按 `e` 鼠标向上移动
- [x] 鼠标模式下按 `d` 鼠标向下移动
- [x] 鼠标模式下按 `s` 鼠标向左移动
- [x] 鼠标模式下按 `f` 鼠标向右移动
- [x] 鼠标模式下按 `w` 执行左键点击
- [x] 鼠标模式下按 `r` 执行右键点击
- [x] 鼠标模式下按 `j` 滚轮向下滚动
- [x] 鼠标模式下按 `k` 滚轮向上滚动
- [x] 鼠标模式下按 `h` 滚轮向左滚动
- [x] 鼠标模式下按 `l` 滚轮向右滚动

## 文档
- [x] README 文档已更新，包含鼠标模式功能说明
