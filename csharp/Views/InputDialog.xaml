<Window x:Class="CapsLockPro.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="输入" Width="380" ResizeMode="NoResize" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="{StaticResource BgBrush}" FontFamily="{StaticResource UiFont}">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" x:Name="PromptText" Text="提示" Foreground="{StaticResource HintTextBrush}" Margin="0,0,0,8"/>
        <TextBox Grid.Row="1" x:Name="ValueBox" MinHeight="30"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Style="{StaticResource BtnPrimary}" x:Name="OkBtn" Content="确定" MinWidth="80" Margin="0,0,8,0" IsDefault="True" Click="Ok_Click"/>
            <Button Style="{StaticResource Btn}" x:Name="CancelBtn" Content="取消" MinWidth="80" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
